# Favicon Notice

The main favicon is served as an SVG file (favicon.svg) for modern browsers.
For browsers that need ICO format, you can convert the SVG to ICO using online tools
or image editing software and save it as favicon.ico in this directory.

Current favicon: favicon.svg (32x32 SVG format with pinkish-purple gradient theme)